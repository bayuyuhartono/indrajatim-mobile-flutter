name: indrajatim
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.11.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.4
  jiffy: ^5.0.0
  flutter_html: ^2.2.1
  flutter_launcher_icons: ^0.9.3
  cached_network_image: ^3.2.0
  shimmer: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/full-square-logoijt.png"

flutter:

  uses-material-design: true

  assets:
    - assets/images/
